@*model IEnumerable<BrewRecipeBox.Models.IngredientSummary>*@

@{
    ViewBag.Title = "IngredientSummary";
}
@Html.RenderPartial("_IngredientSummary", Model as IEnumerable<BrewRecipeBox.Models.IngredientSummary>);
@*
@foreach (var item in Model)
{ 
    @Html.Display(item.Name);
    @*Html.RenderPartial("_IngredientSummary", item);*@
}
@*
<h2>RecipeSummary</h>2

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Style)
        </th>
        <th></th>
    </tr>

@foreach (var item in ViewData["RecipeSummary"] as IEnumerable<RecipeSummary>) {
    <tr>
        <td>
            @Html.DisplayFor(item.Name)           
        </td>
        <td>
            @Html.DisplayFor(item.Style)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
    <th>
    @Html.DisplayNameFor(item.IngredientList)
    </th>

foreach (var subItem in item.IngredientList){
    <tr>
        <td>
            @Html.Display(subItem.Name)
        </td>
        <td>
            @*Html.Display(subItem.Quantity)
        </td>
        <td>
            @Html.Display(subItem.Unit)
        </td>
    </tr>
        
}
}
</table>
*@